// Author(s): Paul Calande
// An enum for describing corners.

public enum Corner
{
    TopLeft,
    TopRight,
    BottomLeft,
    BottomRight,
    Northwest = TopLeft,
    Northeast = TopRight,
    Southwest = BottomLeft,
    Southeast = BottomRight
}